name: New issue
on: 
  issues:
    types: [opened, edited] 
    
jobs:
  output-issue:
    name: Output new issue
    runs-on: ubuntu-latest
    steps:
      - name: Print context
        run: echo "new_issue=${{ toJSON(github) }}"V
      - name: Print issue details
        run: |
          echo "New issue called: ${{ toJSON(github).issue.title }} was created:"
          echo "\t created_at:${{ toJSON(github).issue.created_at }}"
          echo "\t url:${{ toJSON(github).issue.url }}"
          echo "\t description:${{ toJSON(github).issue.body }}"
        