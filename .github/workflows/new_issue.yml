name: New issue
on: 
  issues:
    types: [opened, edited] 
    
jobs:
  output-issue:
    name: Output new issue
    runs-on: ubuntu-latest
    steps:
      - name: Print context
        run: echo "${{ toJSON(github) }}"